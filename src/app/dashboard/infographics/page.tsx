import type { Metadata } from "next";
import Workspace from "@/components/dashboard/Workspace";

export const metadata: Metadata = { title: "Marketplace Infographics — Artboard" };

export default function InfographicsPage() {
  return <Workspace />;
}
